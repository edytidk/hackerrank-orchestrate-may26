# Copy this file to .env for local runs. Never commit .env.

# Required only when using LLM response generation.
OPENAI_API_KEY=

# Optional. Defaults to gpt-4o-mini in code/generator.py.
OPENAI_MODEL=gpt-4o-mini
